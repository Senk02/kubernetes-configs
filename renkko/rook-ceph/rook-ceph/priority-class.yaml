apiVersion: scheduling.k8s.io/v1
kind: PriorityClass
metadata:
  name: storage-critical
value: 2000
preemptionPolicy: PreemptLowerPriority
globalDefault: false
description: "Critical priority for storage (Ceph OSD) pods that need to preempt other pods"
