apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cilium-application
  namespace: argocd
spec:
  destination:
    namespace: kube-system # or the namespace you want cilium to be in
    server: https://kubernetes.default.svc
  project: default
  source:
    chart: cilium
    repoURL: https://helm.cilium.io/
    targetRevision: 1.17.2 # or the version you want
    helm:
      valuesObject:
        ipam:
          mode: kubernetes
        kubeProxyReplacement: true
        # Keep L7 proxy ports stable across a delayed agent restart. At the 15m
        # default the checkpoint goes stale, so the agent reallocates ports while
        # the separate cilium-envoy DaemonSet keeps serving the old listeners.
        extraArgs:
          - --restored-proxy-ports-age-limit=1440
        securityContext:
          capabilities:
            ciliumAgent:
              - CHOWN
              - KILL
              - NET_ADMIN
              - NET_RAW
              - IPC_LOCK
              - SYS_ADMIN
              - SYS_RESOURCE
              - DAC_OVERRIDE
              - FOWNER
              - SETGID
              - SETUID
            cleanCiliumState:
              - NET_ADMIN
              - SYS_ADMIN
              - SYS_RESOURCE
        cgroup:
          autoMount:
            enabled: false
          hostRoot: /sys/fs/cgroup
        k8sServiceHost: localhost
        k8sServicePort: 7445
        l2announcements:
          enabled: true
        priorityClassName: network-critical
        operator:
          priorityClassName: network-critical
        gatewayAPI:
          enabled: true
          externalTrafficPolicy: Local
          secretsNamespace:
            create: false
            name: certificate
  syncPolicy:
    automated:
      allowEmpty: true
      prune: true
      selfHeal: true
