apiVersion: routeros.sindrip.io/v1beta1
kind: ProviderConfig
metadata:
  name: default
spec:
  credentials:
    source: Secret
    secretRef:
      name: routeros-credentials
      namespace: crossplane-system
      key: credentials
