apiVersion: ceph.rook.io/v1
kind: CephBlockPool
metadata:
  name: replicapool-ssd
  namespace: rook-ceph
spec:
  failureDomain: osd
  deviceClass: ssd
  replicated:
    size: 2
  parameters:
    compression_mode: none
