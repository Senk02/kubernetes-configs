apiVersion: ceph.rook.io/v1
kind: CephFilesystem
metadata:
  name: owofs
  namespace: rook-ceph
spec:
  metadataPool:
    failureDomain: osd
    deviceClass: hdd
    replicated:
      size: 2
  dataPools:
    - name: replicated
      failureDomain: osd
      deviceClass: hdd
      replicated:
        size: 2
  preserveFilesystemOnDelete: true
  metadataServer:
    activeCount: 1
    activeStandby: true
    resources:
      limits:
        memory: "1024Mi"
      requests:
        cpu: "500m"
        memory: "1024Mi"
