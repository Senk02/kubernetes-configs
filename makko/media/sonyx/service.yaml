apiVersion: v1
kind: Service
metadata:
  name: sonyx
spec:
  selector:
    app: sonyx
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  - protocol: TCP
    port: 8000
    targetPort: 8000
  type: ClusterIP