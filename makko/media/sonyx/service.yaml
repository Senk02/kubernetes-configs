apiVersion: v1
kind: Service
metadata:
  name: sonyx
spec:
  selector:
    app: sonyx
  ports:
  - protocol: TCP
    name: http
    port: 80
    targetPort: 80
  - protocol: TCP
    name: eightk
    port: 8000
    targetPort: 8000
  type: ClusterIP