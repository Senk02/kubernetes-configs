apiVersion: v1
kind: Service
metadata:
  name: sonyx
spec:
  selector:
    app: sonyx
  ports:
  - protocol: TCP
    name: http
    port: 80
    targetPort: 80
  type: ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  name: sonyx-database
  labels:
    app: sonyx-database
spec:
  ports:
  - port: 5432
    targetPort: 5432
  selector:
    app: sonyx-database
  type: ClusterIP